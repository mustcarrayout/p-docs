
```js

// web local path
var request = $WEB.getParamsTable().getHttpRequest();
println(request.getSession().getServletContext().getRealPath("/"));
String objectName = fullPath.substring(fullPath.indexOf("/uploads")+1);

var uuid = new Packages.cn.myapps.util.sequence.Sequence().getSequence();

var URLConnector = createObject("cn.myapps.util.http.URLConnector");
println("====url===" + url);
var remoteUrl = URLConnector.getDataAsFileUrl(url);

```

DATASOURCE_NAME
APPLICATION_ID


保租房